model pluto {
  metadata {
      count: 10
      protofile: "./samples/pluto.proto"
      tags: {
          "service":  "pluto",
          "team_name":  "nimbus"
      }
  }

  misc {
    const Count = 10000
  }

  fields {
    age() int
    next_page() int
    set_name() string
  }

  gens {
    func age() {
      return iter + 18
    }

    func next_page() {
      return IntBetween(1, Count)
    }

    func set_name() {
      return "user_data"
    }
  }
}